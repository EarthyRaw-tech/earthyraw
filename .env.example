# Required: admin auth has no fallback defaults
ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=change-me-too

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Earthy Raw <no-reply@example.com>"

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Required for admin image uploads and optional JSON snapshot backups on Vercel Blob
BLOB_READ_WRITE_TOKEN=
